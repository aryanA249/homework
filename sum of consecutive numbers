
#include <stdio.h>

int main() {
    int n, sum , i = 1;

    printf("Enter a number: ");
    scanf("%d", &n);

    while (i <= n) {
        sum += i;
        i++;
    }

    printf("The sum of consecutive numbers from 1 to %d is: %d\n", n, sum);

    return 0;
}
